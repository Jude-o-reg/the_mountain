This solution adds the numbers seen in the view grid based on the direction they are based on the centre then jump in the direction that has the highest number. then see if the peak is in the new grid. If it is declare that point as the peak. If not count the numbers in the grid again and go in in the direction with highest numbers again. repeat until the peak is found.

note: the header file and algorithm for making the mountain were not maid by me and are used by my solution file 
